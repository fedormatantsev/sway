[package]
name = "sway-graph"
version.workspace = true
edition.workspace = true

# Spec §2: bevy_app/ecs/math/reflect/time/transform only. NOT the `bevy`
# facade, NOT bevy_render. bevy_asset joined at M4, as anticipated. This
# manifest is the only place that constraint is enforced.
# ron and serde for the project format only (Task 1).
[dependencies]
bevy_app.workspace = true
bevy_asset.workspace = true
bevy_ecs.workspace = true
bevy_math.workspace = true
bevy_reflect.workspace = true
bevy_time.workspace = true
bevy_transform.workspace = true
ron.workspace = true
serde.workspace = true
