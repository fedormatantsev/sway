[package]
name = "sway-midi"
edition.workspace = true
version.workspace = true

[dependencies]
sway-midi-core.workspace = true
sway-graph.workspace = true
sway-events.workspace = true
# Trigger lives on the generic signal layer. This crate names it so OnMidiNote
# can fire it; it does not depend on runtime or any other peer domain.
sway-base-nodes.workspace = true
bevy_app.workspace = true
bevy_ecs.workspace = true
bevy_reflect.workspace = true
bevy_time.workspace = true
crossbeam-channel.workspace = true

[dev-dependencies]
# The tick harness, rather than a copy of it in this crate.
sway-graph = { workspace = true, features = ["test-support"] }
