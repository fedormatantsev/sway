[package]
name = "sway-geo"
version.workspace = true
edition.workspace = true

# Dormant: CPU-side geometry cooks, no node kinds and no plugin yet. It
# depends on nothing of this project's — whether it is the landing site for
# the post-MVP geometry work or should be deleted is a roadmap question
# (§10), not a structure one.
[dependencies]
bevy_ecs.workspace = true
bevy_math.workspace = true
bevy_reflect.workspace = true
